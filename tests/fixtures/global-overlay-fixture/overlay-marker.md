# Overlay fixture marker

Synthetic overlay file used by test_global_layer.bats (G-2) to prove the
entrypoint's overlay-copy step runs, without depending on any real
per-image overlay (e.g. global-crypto/) existing in the repo yet.
